<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  	<%# Rails automatically looks for a partial called _user.html.erb  %>
  	<%= render @users %>
</ul>

<%= will_paginate %>